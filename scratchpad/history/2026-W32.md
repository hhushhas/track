# 2026-W32 (Aug 3–9) — digest

Source: 2 session logs (`store-review-monitor-*-08-03`,
`mobile-preview-*-08-07`), condensed 2026-08-23.

## Track 1.2.0 store review monitoring (08-03)
**Google Play reached a terminal outcome:** production 1.2.0 / versionCode 14
went live at 100% in 177/177 countries on 3 Aug 11:53 PKT. Closed alpha carries
the same versionCode; open testing is paused and superseded by production;
internal still serving. No unpublished changes, managed publishing off.

**Apple did not:** version 1.2.0 and review submission
`69ff86d7-4089-4be7-b4a1-3e51bd3d58fb` stayed `WAITING_FOR_REVIEW` across every
check. Build 28 remained `VALID` / `IN_BETA_TESTING`, attached to the version and
linked to the `Internal Testers` group; release type `AFTER_APPROVAL`, so no
manual release control was pending.

No store action was ever required. The run was dominated by tooling failure —
coordinator report delivery hung repeatedly and could not be verified, the
automation directory disappeared mid-run, and the automation-view call hung past
30s. Complaints #3341, #3342, #3343, #3349, #3350, #3351 filed. Hasan asked to
delete the monitor at 14:46; the app returned `deleteStatus: not_found`,
confirming `monitor-track-1-2-0-store-reviews` was already gone.

## Mobile web preview (08-07)
Got a local design preview running for `apps/mobile`. No iOS simulator or Android
device was available, and Expo deps were not installed. After installing with
lifecycle scripts disabled, Expo web bundled but failed static rendering with
`Invalid hook call` — the hoisted workspace resolves multiple React copies. Added
an opt-in `EXPO_WEB_OUTPUT` override in `apps/mobile/app.config.ts` so local
preview can use `single` while configured static output stays the default.
Relaunched with dev values injected from the `dev` 1Password item via `--reveal`
(nothing written to repo or logs); `localhost:8081/sign-in` returned HTTP 200 and
rendered. Focused typecheck and lint passed. Idle Expo ~483 MB RSS.
