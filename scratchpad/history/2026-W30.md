# 2026-W30 (Jul 20–26) — digest

Source: 2 session logs (`message-deletion-*-07-23`,
`remove-umaymah-jamal-*-07-23`), condensed 2026-08-23.

## Message deletion feature (committed `0e4b4f8`)
Implemented author-only message deletion with exact represented-membership
authorization, attachment and task-evidence cleanup, body-free deletion audit
events, and thread metadata recalculation. Confirmed delete actions added to web
and mobile Channel timelines and threads.

Verification moved to the Mac mini partway through: production web build, clean
production audit, and Expo exports for iOS/Android/web all passed. The iOS
development client exercised deletion end to end in the authenticated app
(author a message → action sheet → destructive alert → message gone). Android
release APK installed on a fresh emulator but had no authenticated session, so
native end-to-end coverage is **iOS only**. The web path could not be exercised
interactively because the demo user's password no longer matched the configured
dev bypass; focused web interaction coverage was added instead.

Code review caught two real gaps: archived-thread deletion was not revalidated
server-side, and the represented-Company web timeline lacked the action. Both
fixed — deletion is now hidden in archived Channel timelines and stored assistant
evidence references are purged. Full gate rerun clean (285 tests at the time).

## Production membership removal (authorized manual op)
Removed Umaymah Jamal from the Tuition Highway project and its Marketing channel.
The UI path failed first — the signed-in production account exposed only two
`Q9 Track` projects and `groups:listVisible` returned a server error (friction
report #2719). Completed instead via explicitly authorized production Convex CLI
against `fleet-manatee-941`: a guarded transactional maintenance mutation removed
the project and channel memberships, cleared task-scope references, and wrote both
audit events. The temporary function was removed from production and verified
absent. Final checks: zero project memberships, zero channel memberships, zero
assigned tasks, both audit events present, `projects:list` returns `[]`.
