# 2026-W33 (Aug 10–16) — digest

Source: 1 session log (`codex-track-main-flow-dogfood-*-08-14`), condensed
2026-08-23.

## Main-flow dogfood
Full manual pass over Track's primary workflow on local web + Convex dev.

**Blocker found and fixed first:** the configured development auth URL redirected
successful sign-in to the Convex host, so local sign-in appeared blank. Corrected
by setting the dev deployment's `SITE_URL`, `BETTER_AUTH_URL`, and
`DEV_AUTH_BYPASS` to the documented loopback values.

First pass covered invalid email handling, account creation, profile setup,
message sending, task creation from message evidence, board navigation, and task
editing — the task route worked once the auth redirect was corrected. Second pass
covered task comments, subtasks, My tasks, Inbox, search, and a clean
message → task → board recording.

**No app-level workflow defect was found** after the auth setup correction. The
final recording was normalized and uploaded to the Drive `recordings` folder;
anonymous link check returned HTTP 200 after propagation.
