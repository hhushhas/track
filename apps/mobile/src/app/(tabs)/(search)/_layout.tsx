import { PrimaryStack } from '@/components/primary-stack';

export const unstable_settings = { initialRouteName: 'search' };

export default function SearchStackLayout() {
  return <PrimaryStack initialRouteName="search" />;
}
