import { PrimaryStack } from '@/components/primary-stack';

export const unstable_settings = { initialRouteName: 'projects' };

export default function ProjectsStackLayout() {
  return <PrimaryStack initialRouteName="projects" />;
}
