import { PrimaryStack } from '@/components/primary-stack';

export const unstable_settings = { initialRouteName: 'inbox' };

export default function InboxStackLayout() {
  return <PrimaryStack initialRouteName="inbox" />;
}
